---
- name: Test
  hosts: local
  become: true
  vars_files:
    - ./variables.yml
  handlers:
    - name: apt-update
      ansible.builtin.apt:
        update_cache: true
  roles:
    #- common
    #- git
    #- php
    #- docker
    #- vscode
    #- chrome
    #- firefox
    - snap

    #
    # - node
